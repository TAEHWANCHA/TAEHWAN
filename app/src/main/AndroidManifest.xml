<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a15n540_ut50k.project1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".teamleaderactivity" android:label="팀 리더정하기" ></activity>
        <activity android:name=".meetingact" android:label="미팅 일정/조정"></activity>
        <activity android:name=".distributeact" android:label="역할 분배"></activity>
        <activity android:name=".progact" android:label="진행 상황"></activity>
        <activity android:name=".gonguact" android:label="파일공유 게시판"></activity>
        <activity android:name=".msgAct" android:label="자유게시판"></activity>
        <activity android:name=".adminAct" android:label="관리자"></activity>
    </application>

</manifest>